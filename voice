<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <title>Voice Input Button</title>
    <style>
      #main-container {
        text-align: center;
        border: 1px solid black;
        padding: 1em;
        font-family: Arial, sans-serif;
      }
      button {
        padding: 10px 20px;
        font-size: 16px;
        margin: 10px;
        cursor: pointer;
        background-color: #4285f4;
        color: white;
        border: none;
        border-radius: 4px;
      }
      button:hover {
        background-color: #3367d6;
      }
      #input-area, #output-area {
        min-height: 20px;
        margin: 10px 0;
        padding: 10px;
        border: 1px solid #ddd;
        border-radius: 4px;
        text-align: left;
      }
    </style>
  </head>
  <body>
    <div id="main-container">
      <h2>Voice Recognition</h2>
      <p id="input-area">...</p>
      <p id="output-area">...</p>
      <button id="listen-button" onclick="listen()">🎤 Speak</button>
    </div>

    <script>
      function listen() {
        let inputArea = document.getElementById('input-area');
        let outputArea = document.getElementById('output-area');

        // Check if browser supports speech recognition
        if ('webkitSpeechRecognition' in window || 'SpeechRecognition' in window) {
          var recognition = new (window.SpeechRecognition || window.webkitSpeechRecognition)();
          recognition.lang = "en-US";
          recognition.continuous = false;
          recognition.interimResults = false;

          recognition.onresult = function(event) {
            let transcript = event.results[0][0].transcript;
            inputArea.innerHTML = "You said: " + transcript;
            
            // Simple response logic
            if (transcript.toLowerCase().includes("hello")) {
              outputArea.innerHTML = "Hello! How can I help you?";
            } else if (transcript.toLowerCase().includes("weather")) {
              outputArea.innerHTML = "Opening weather information...";
              window.open("https://www.google.com/search?q=weather", "_blank");
            } else {
              outputArea.innerHTML = "I heard: " + transcript + ". I'm not sure how to respond.";
            }
          };

          recognition.onerror = function(event) {
            outputArea.innerHTML = "Error occurred in recognition: " + event.error;
          };

          recognition.onend = function() {
            console.log("Speech recognition ended");
          };

          recognition.start();
          outputArea.innerHTML = "Listening...";
        } else {
          outputArea.innerHTML = "Sorry, your browser does not support speech recognition.";
        }
      }
    </script>
  </body>
</html>
